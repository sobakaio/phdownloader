{
  "name": "phdownloader",
  "version": "1.2.3",
  "private": true,
  "description": "Chrome MV3 extension: download premium PH videos. This package only carries the Node unit-test tooling for the lib/ parsers.",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  }
}
